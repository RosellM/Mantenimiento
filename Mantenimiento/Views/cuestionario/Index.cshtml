@model Mantenimiento.Models.cuestionarioModel
@{
    ViewBag.Title = "Cuestionarios";
    if (ViewBag.user != null)
    {
        Model.flag = 1;
        Layout = null;
        @Html.Partial("_pUser")
    }


}

<h1>Elije el test</h1>

    <table class="table table-striped" style="max-width:960px; text-align:center; margin:2.53em auto;" id="toremove">
        <tbody>
            <tr class="warning">
                <td> Nombre del Test </td>
               
                <td colspan="2"> <center>Opciones</center> </td>
            </tr>
           @foreach (var m in Model.ListarTest())
            {
                <tr>  
                    <td> @m.nombre </td>
                    <td> <center><button onclick="location.href='@Url.Action("Secciones", "cuestionario", new { id = m.id,Model.flag})';" class=" btn btn-info"><span class="glyphicon glyphicon-pencil">INICIAR</span></button> </center></td>
                </tr>
            }
        </tbody>
    </table>
